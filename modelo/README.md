# Pasta modelo — referência única CF

- `01-quadrado-cheio/` → modelo de referência com identidade do site (classe `modelo-quadrado`).
- Não colocar modelos de uso real aqui, apenas este de referência.

Conteúdo de `01-quadrado-cheio/`:
- `info.json` → nome, descrição, classe, tema
- `preview.html` → prévia isolada com dados da Ana Marcia
- `estilo.css` → só o CSS desse modelo
